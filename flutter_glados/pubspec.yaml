name: flutter_glados
description: Glados for Flutter.
version: 0.0.1
homepage: https://github.com/marcelgarus/glados

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  glados:
    git:
      url: https://github.com/rizumita/glados.git
      ref: export-for-widget-test
  meta: ^1.3.0

dev_dependencies:
  flutter_lints: ^2.0.0
